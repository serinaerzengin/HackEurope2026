[project]
name = "backend"
version = "0.1.0"
description = "HackEurope2026 backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
]

[tool.setuptools.packages.find]
include = ["api*", "agents*"]
