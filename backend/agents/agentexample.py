def run_example_agent(user_message: str) -> str:
    """
    Dummy agent example.
    Replace this with your actual agent logic (e.g. interviewer, evaluator, etc.)
    """
    return f"Echo: {user_message}"
